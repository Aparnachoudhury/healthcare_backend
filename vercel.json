{
  "buildCommand": "cd health-dashboard && npm install && npm run build",
  "outputDirectory": "health-dashboard/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
